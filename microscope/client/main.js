// 4.4 Subscribe to collections/subsets on client
//	 	subscriptions must correspond to named publications on server

// 5.2 Moved this into the Router 'waitOn' hook
//     to guarantee that the subscription completes before route shown
// Meteor.subscribe('posts');